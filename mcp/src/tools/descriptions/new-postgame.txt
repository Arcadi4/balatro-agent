Create a new post-game analysis for a finished run. Returns the analysis index (postgame://<index>) and the created file path. Write content as strategic analysis — which strategy carried the run, decisive decisions, mistakes, and lessons transferable to later runs — not a chronological record of plays. One document per finished run; never modify an existing postgame://<index> unless explicitly instructed.
